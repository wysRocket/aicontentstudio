{
  "servers": {
    "hostinger-api": {
      "command": "npx",
      "args": [
        "-y",
        "hostinger-api-mcp"
      ],
      "env": {
        "DEBUG": "false",
        "API_TOKEN": "${env:HOSTINGER_API_TOKEN}"
      }
    }
  }
}